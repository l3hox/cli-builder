[package]
name = "mock-adapter"
version = "0.1.0"
edition = "2021"
description = "Cross-platform mock SDK adapter for cli-builder integration tests"
license = "EUPL-1.2"
publish = false

[[bin]]
name = "mock-adapter"
path = "src/main.rs"

[dev-dependencies]
cli-builder-core = { path = "../core" }
serde_json = { workspace = true }
