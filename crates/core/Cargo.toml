[package]
name = "cli-builder-core"
version = "0.2.0"
edition = "2021"
description = "Shared core for cli-builder generators — SdkMetadata models, ModelMapper, ParameterFlattener"
license = "EUPL-1.2"

[features]
test-support = []

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
